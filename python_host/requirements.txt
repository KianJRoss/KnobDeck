# Runtime dependencies for KnobDeck host app

# HID communication library
hidapi>=0.14.0

# Windows audio control (Core Audio API)
pycaw>=20220416

# Windows API access (window management, media keys)
pywin32>=305

# COM types (required by pycaw)
comtypes>=1.2.0

# System tray icon
pystray>=0.19.0
Pillow>=12.2.0

# Process and system utilities (for context detection)
psutil>=5.9.0

# Modern Qt-based overlay UI
PyQt6>=6.6.0

# Global hotkey listener (encoder events delivered via Ctrl+Alt+F13-F18)
keyboard>=0.13.5
